# Prices Parser

Spring Boot приложение для парсинга информации о товарах (название, цена, описание) с сайта pitergsm.ru и сохранения результатов в базу данных.

## Описание

При запуске приложение автоматически начинает мультипоточный парсинг заранее заданного массива из 145 ссылок на товары с сайта **pitergsm.ru** (файл `product_links.json`). Парсинг происходит батчами по 5 товаров с задержкой 3 секунды между батчами. Все результаты сохраняются в базу данных H2 и доступны через REST API.

**Важно:** Приложение настроено специально для парсинга товаров с сайта **pitergsm.ru** и корректно работает только с этим доменом.

## Возможности

- Автоматический парсинг товаров при старте приложения
- Мультипоточная обработка с задержками между запросами
- RESTful API для получения результатов и добавления новых ссылок
- In-memory база данных H2 с веб-консолью для просмотра
- Настраиваемый пул потоков (5-10 потоков)
- Фильтрация, сортировка и пагинация товаров

## Требования

- Java 21
- Maven 3.6+

## Установка и запуск

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd prices-parser
```

2. Соберите приложение:

```bash
mvn clean install
```

3. Запустите приложение:

```bash
mvn spring-boot:run
```

Приложение запустится по адресу `http://localhost:8080`

После запуска приложение автоматически начнет парсинг товаров из файла `product_links.json`. Через несколько минут результаты будут доступны через API и в базе данных.

## Быстрый старт

1. Запустите приложение: `mvn spring-boot:run`
2. Подождите 1-2 минуты, пока приложение спарсит первые товары
3. Откройте в браузере: `http://localhost:8080/api/results` - увидите JSON с товарами
4. Откройте веб-консоль БД: `http://localhost:8080/h2-console` (параметры подключения см. ниже)
5. В консоли БД выполните: `SELECT * FROM PRODUCTS` - увидите все спарсенные товары

## API эндпоинты

### ⭐ Получение результатов парсинга (главный эндпоинт)

Получение всех спарсенных товаров с пагинацией и сортировкой:

**GET** `/api/results`

Параметры запроса:

- `page` - номер страницы (по умолчанию: 0)
- `size` - количество элементов на странице (по умолчанию: 10)
- `sortBy` - поле для сортировки (по умолчанию: "id")
- `sortDir` - направление сортировки: "asc" или "desc" (по умолчанию: "asc")

Пример:

```bash
curl "http://localhost:8080/api/results?page=0&size=20&sortBy=price&sortDir=desc"
```

Пример ответа:

```json
{
  "content": [
    {
      "id": 1,
      "url": "https://example.com/product",
      "title": "Название товара",
      "price": 99.99,
      "description": "Описание товара",
      "parsedAt": "2025-11-23T10:30:00"
    }
  ],
  "totalElements": 100,
  "totalPages": 5,
  "size": 20,
  "number": 0
}
```

### Парсинг дополнительных товаров

Ручной запуск парсинга для дополнительных товаров с pitergsm.ru. Используется для проверки отдельных ссылок:

**POST** `/api/parse`

Тело запроса:

```json
{
  "urls": [
    "https://pitergsm.ru/catalog/phones/xiaomi/redmi-note-14/smartfon-xiaomi-redmi-note-14-6-128-gb-chernyy/",
    "https://pitergsm.ru/catalog/phones/samsung/galaxy-a/galaxy-a26/smartfon-samsung-galaxy-a26-6-128gb-belyy/"
  ]
}
```

Ответ: `202 Accepted` - парсинг запущен асинхронно

**Примечание:** Работает только с ссылками на товары с сайта **pitergsm.ru**. Ссылки на другие домены не будут корректно обработаны.

## 💾 Доступ к базе данных

Приложение использует in-memory базу данных H2. Вы можете просмотреть все спарсенные товары и выполнить SQL-запросы через веб-консоль в браузере:

**URL:** `http://localhost:8080/h2-console`

**Параметры подключения:**

- JDBC URL: `jdbc:h2:mem:pricesdb`
- Username: `sa`
- Password: _(оставьте пустым)_

Нажмите "Connect" для доступа к базе данных. После подключения вы можете:

- Просматривать таблицу `PRODUCTS` со всеми товарами
- Выполнять SQL-запросы (например: `SELECT * FROM PRODUCTS ORDER BY PRICE DESC`)
- Проверять статистику парсинга (`SELECT COUNT(*) FROM PRODUCTS`)

## Конфигурация

Настройки приложения можно изменить в файле `src/main/resources/application.properties`:

- Порт сервера: `server.port=8080`
- Размер пула потоков: `parser.thread-pool.core-size=5`, `parser.thread-pool.max-size=10`
- Настройки базы данных: конфигурация H2
- Уровни логирования

Для изменения списка ссылок для автоматического парсинга отредактируйте файл `src/main/resources/product_links.json`.

Настройки парсинга в `ScheduledTaskService.java`:

- `BATCH_SIZE = 2` - количество товаров в одном батче
- `DELAY_BETWEEN_BATCHES_MS = 10000` - задержка между батчами (10 секунд)

## Структура проекта

```
src/main/java/com/pricesparser/
├── client/          # HTTP клиенты для внешних запросов
├── config/          # Классы конфигурации Spring
├── controller/      # REST API контроллеры
├── dto/             # Объекты передачи данных
├── model/           # JPA сущности
├── parser/          # Логика парсинга веб-страниц
├── queue/           # Управление очередью URL
├── repository/      # JPA репозитории
├── service/         # Бизнес-логика сервисов
├── task/            # Определения асинхронных задач
└── util/            # Утилитарные классы
```

## Тестирование

Запуск тестов:

```bash
mvn test
```

## Технологии

- Spring Boot 3.2.0
- Spring Data JPA
- Spring WebFlux
- H2 Database
- Jsoup (HTML parser)
- OpenFeign
- Maven
